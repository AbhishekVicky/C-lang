#include <stdio.h>

int main(){
    int marks [90];  // Reserve space to store 90 integers

     marks[0] = 55;
     marks[1] = 23;
     marks[2] = 98;
    // We can go all the way till from marks[0] to marks[89].

    printf("Marks 0 is %d and 1 is  %d ", marks[0], marks[1]);

    return 0;
}